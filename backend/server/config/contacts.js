/* Public website contact information — not a secret, so this is a plain
   constant rather than an environment variable. Single source of truth so
   the accommodation coordinator's details only need updating in one place. */
const ACCOMMODATION_CONTACT = {
  name: "Patience Makaji Mwangeka",
  position: "Accommodation and Hospitality Coordinator",
  phoneDisplay: "+254 714 959811"
};

module.exports = { ACCOMMODATION_CONTACT };
